import { Forum } from "@/api/forum/interfaces";

const CustomLinks = (props: {forum: Forum}) => {
    return (
        <>
            <h1>Custom Links</h1>
        </>
    );
}

export default CustomLinks;